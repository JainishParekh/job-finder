from enum import Enum

class Constants(Enum):
    #----------------------------------------------
    # Literals constants
    #----------------------------------------------

    SUCCESS = "success"
    FAILURE = "failure" 